---
description: Deduplicate Global Event Listeners (LOW)
globs: src/popup/**/*.{tsx,ts,jsx,js},src/options/**/*.{tsx,ts,jsx,js}
alwaysApply: false
---

Follow `.cursor/skills/react-best-practices/rules/client-event-listeners.md` verbatim / as adapted in that file.

That skill rule is the sole source of truth. Do not paste or maintain a duplicate rule body in this `.mdc` — this file is only a Cursor glob trigger (`description` / `globs` / `alwaysApply`).
