---
description: >-
  Chrome Web Store extension quality — MV3, permissions, privacy disclosures,
  security, bfcache/performance, and UX for store review. Use when editing
  extension runtime, manifest, privacy policy, inject scripts, or preparing
  a CWS submission. Not for React UI performance (see react-bp-* rules).
globs: src/**/*.{ts,tsx,js,jsx},public/**/*.{js,css,html},src/manifest.json,PRIVACY.md
alwaysApply: false
---

Follow `.cursor/skills/chrome-web-store-best-practices/SKILL.md` (Extension quality / privacy / MV3 / bfcache / UX) and `.cursor/skills/chrome-web-store-best-practices/reference.md` (pre-submit gate, Privacy tab, permissions notes) as the canonical guidance for Chrome Web Store extension quality.

Skill docs are the sole source of truth. Do not paste or maintain duplicate checklist content in this `.mdc` — this file is only a Cursor glob trigger (`description` / `globs` / `alwaysApply`).
